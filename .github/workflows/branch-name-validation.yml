name: Branch Name Validation

on: [ push, pull_request ]

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate branch name
        run: bash scripts/validate_branch_name.sh